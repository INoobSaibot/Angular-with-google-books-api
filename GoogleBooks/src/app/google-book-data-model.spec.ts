import { GoogleBookDataModel } from './google-book-data-model';

describe('GoogleBookDataModel', () => {
  it('should create an instance', () => {
    expect(new GoogleBookDataModel()).toBeTruthy();
  });
});
